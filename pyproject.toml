[project]
name = "commonlib"
version = "1.0.0"
description = "Reusable common utilities for Airflow projects"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
